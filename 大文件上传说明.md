# 基于 Netty 的大文件传输说明
---
说明下:如果客户端那边的是顺序发的，那下面排序缓冲那部分就不需要了。
一个最简单的例子在一个循环里面用一个线程发送,tcp是一个有序的协议，所以就不要考虑排序缓冲区，就合并落盘就可以了
因为这个是在特定的场景内,如果你的带宽升级了,例如：到了1G,或者10G或者是内网,那你得重新考虑参数的调整
                                          可能单线程的顺序写这里重新考虑，因为瓶颈可能不在是带宽了

---

## 一、场景与约束

| 项目 | 值 | 说明 |
|------|-----|------|
| 专线带宽 | 100 Mbps | 预留 80 Mbps 给文件传输 |
| 传输速率上限 | 10 MB/s | 80 Mbps ÷ 8 |
| RTT | 50 ms | 公网 ping 测量值 |
| BDP | 512 KB | 10 MB/s × 0.05 s |
| TCP 窗口 | 600 KB | BDP 上取整留余量 |
| Chunk 大小 | 1 MB | 对齐内存，平衡系统调用频率与丢包风险 |
| 磁盘写入速度 | 300 MB/s | 远大于网络速度，不会成为瓶颈 |

硬件：4 核 8 线程 16G 内存，100M 专线宽带。

---

## 二、协议设计
### 2.1 为什么不用 HTTP/WebSocket
HTTP 协议头（文本格式）开销大，WebSocket 有额外的帧封装。自定义二进制协议将协议头压缩到 56 字节固定 + 可变文件名，1MB 数据块下协议开销仅约 0.01%。
### 2.2 二进制协议头 (56 字节，大端序)
```
偏移  大小   字段              说明
────  ────   ────────────────  ──────────────────────────
0     4      Magic: 0x4E465446 "NFTF"，校验包合法性
4     1      Version(高4位)    位压缩：版本和类型合占 1 字节
             PacketType(低4位)
5     1      Flags             bit0=LastChunk, bit1=Retransmit, bit2=ACK
6     2      FileNameLength    uint16
8     8      FileID            int64
16    4      ChunkIndex        int32
20    4      PayloadSize       int32
24    8      Offset            当前块在文件中的字节偏移
32    8      NextExpectedOffset 期待的下一个偏移 (ACK/握手复用)
40    16     MD5               载荷 MD5 校验和
56    N      FileName          UTF-8 编码
56+N  M      Payload           数据载荷
```
### 2.3 包类型（用于位运算的 mask 值）
```
HANDSHAKE(0x01)         Client → Server   发起传输，携带文件元信息
HANDSHAKE_RESPONSE(0x02) Server → Client   握手响应，携带续传偏移
DATA(0x03)              双向              数据块
ACK(0x04)               双向              确认已收到的连续字节数
NACK(0x05)              Server → Client   请求重传指定块
COMPLETE(0x06)          双向              传输完成通知
```
### 2.4 位压缩设计
```
Byte 4:   Version (高 4 bits) | PacketType (低 4 bits)
          两个 4-bit 字段压缩进 1 字节，节省 1 字节
Byte 5:   Flags: bit0=LastChunk, bit1=Retransmit, bit2=ACK
          3 个布尔值压缩进 1 字节，节省 2 字节
```
---

### 3.1 整体启动流程
```
main()
  → new FileTransferServer("./received_files")
  → server.start(8080)
Pipeline (bottom → top):
  ① ChannelTrafficShapingHandler  — 流量整形，入站限速 10MB/s
  ② Decoder                       — bytes → FileTransferPacket
  ③ Encoder                       — FileTransferPacket → bytes
  ④ ServerChannelHandler          — 业务逻辑
```

### 3.2 流控四层架构

```
第4层 (Netty流量整形): ChannelTrafficShapingHandler，入站读取 10MB/s
                       服务端读得慢 → 内核 recv buffer 满 → TCP 窗口缩小
                       → 客户端内核 send buffer 满 → write() 阻塞 → 自动反压
第3层 (应用层):         ReceiveBuffer (服务端排序缓冲，10×1MB)
第2层 (Netty 水位):     写缓冲区水位线
                        HIGH=4MB (BDP): channel.isWritable()=false → 暂停发送
                        LOW=2MB (BDP/2):  channel.isWritable()=true  → 恢复发送
                        水位值取 BDP 而非 TCP buffer(600KB)，预留内核排空窗口
第1层 (TCP 内核):       SO_RCVBUF=600KB, SO_SNDBUF=600KB
                        TCP Window 通告机制自动调节发送速率
```
### 3.3 水位线推导链
```
① BDP = bandwidth × RTT = 10 MB/s × 50 ms = 512 KB
   → 网络管道中最多同时容纳 512KB 数据

② TCP receive buffer = 600 KB
   → 内核套接字缓冲区，略大于 BDP 以吸收抖动
   → 内核通过 TCP window 通告发送端：接收窗口 = 600KB

③ Netty high watermark = 4MB 
   → 当 Netty 应用层写缓冲区积压达到一个 BDP 时，
      channel.isWritable() 返回 false，触发应用层反压
      
④ Netty low watermark = 2M (= high watermark / 2)
   → 写缓冲区降到低水位以下时，isWritable() 恢复为 true
   → 高低水位之间作为滞回区间，避免频繁的暂停/恢复切换
   
```
### 3.4 服务端流量整形 
```
服务端流量整形（本方案采用）：
  服务端控制读取速度 → 内核接收缓冲积满 → TCP 窗口缩小
  → 客户端内核发送缓冲满 → write() 阻塞 → isWritable=false → 暂停
  → 反压通过 TCP 协议栈自动传导到客户端，全链路协同
  
  优势：
  ① Netty 内置，不用手写限速代码
  ② 反压通过 TCP 自动传导，无需应用层协调
  ③ 配置统一在服务端，客户端无需感知限速策略
  ④ 配合 TCP 窗口 + Netty 水位 + ReceiveBuffer 形成多层自适应流控
```

### 3.5 服务端流量整形实现

```java
// FileTransferServer.java — Pipeline 配置
pipeline.addLast("trafficShaper",
    new ChannelTrafficShapingHandler(
        0,                      // 出站不限速（ACK/NACK 很小）
        10 * 1024 * 1024,       // 入站 10MB/s
        1000));                 // 每秒检查一次

// trafficShaper 放在 pipeline 最前面
// 在解码之前就控制 socket 的读取速度
```

---
## 四、核心数据结构：ReceiveBuffer（排序缓冲区）
### 4.1 设计问题
网络乱序导致块到达顺序与发送顺序不一致，需要缓冲区暂存乱序块，等待缺失块到达后批量顺序落盘。
### 4.2 四大数据结构
```
① buffer[10] — 固定大小循环缓冲区，每个槽存一个 1MB 块
② BitSet receivedChunks — 位图，bit N=1 表示第 N 块已收到
   ├─ 幂等性：重复包通过 BitSet 检测，直接丢弃
   └─ 快速判断：O(1) 检查任意块是否已到达
③ Map<chunkIndex → slotIndex> — O(1) 映射
   ├─ 已知块索引，直接找到它在 buffer[10] 中的位置
   └─ 避免每次清空时遍历 10 个槽位查找目标块
④ Deque<Integer> freeSlots — 空闲槽位池
   └─ 插入：freeSlots.poll() → 立即得到空槽
   释放：freeSlots.offer(slot) → 归还
```
### 4.3 offer() 处理流
```
chunk 到达
  │
  ├─ BitSet.get(chunkIndex) == true → DUPLICATE (幂等丢弃)
  │
  ├─ chunkIndex == expectedChunkIndex → EXPECTED (队头块，直接写盘)
  │
  ├─ chunkIndex > expectedChunkIndex → 存入 buffer[slot]
  │     ├─ BitSet.set(chunkIndex)
  │     ├─ chunkIndexToSlot.put(chunkIndex, slot)
  │     └─ 检测到 gap → 启动 100ms NACK 定时器
  │
  └─ freeSlots 为空 → BUFFER_FULL
        └─ BitSet 保留（标记已收到）
        └─ 调用方写入磁盘 (溢出写盘，不丢数据)
```
### 4.4 advanceAndFlushContiguous() — 批量落盘
```
expectedChunkIndex: 5 → while(receivedChunks.get(6)) → while(receivedChunks.get(7)) → ...
  块6: slot = chunkIndexToSlot.remove(6) → buffer[slot] 取出 → 写盘 → freeSlots.offer(slot)
  块7: slot = chunkIndexToSlot.remove(7) → buffer[slot] 取出 → 写盘 → freeSlots.offer(slot)
  块8: slot = null → 溢出写盘的块，已在磁盘 → 跳过
        (BitSet=1 但无 buffer 条目，advance 直接跳过)

  返回 [BufferedChunk(6), BufferedChunk(7)]  ← 调用方一次写这 2 块
  expectedChunkIndex 推进到 10
```

---
## 五、NACK 定时器机制
### 5.1 核心问题
TCP 的重传超时是秒级的。在 50ms RTT 的链路上，2×RTT = 100ms 未到达基本可以判定丢包。应用层做 NACK 远比等 TCP 自己修快。
### 5.2 四个触发点
```
触发点① — 乱序块进缓冲区时首次发现缺口 (handleData)
  case BUFFERED:
      if (receiveBuffer.hasGap()) → scheduleNack(ctx)
触发点② — 缓冲区满，溢出写盘后 (handleData)
  case BUFFER_FULL:
      writeOverflow(...)
      sendNack(...) + scheduleNack(ctx)
触发点③ — 写盘后出现新缺口 (handleExpectedChunk)
  if (hasGap()) → scheduleNack(ctx)
  else → cancelNack()
触发点④ — 定时器自续期 (scheduleNack 内部)
  回调中: if (块仍缺失) → sendNack() + scheduleNack(ctx)
```
### 5.3 scheduleNack() 实现
```
scheduleNack(ctx):
  ① if (nackFuture != null && !nackFuture.isDone()) → return
     幂等闸门：同一时刻最多 1 个 ScheduledFuture
  ② nackFuture = ctx.executor().schedule(() -> {
       // ====== 100ms 后在事件循环线程上执行 ======
       ③ nackFuture = null  // 自清，标记"正在执行"
       ④ if (state != TRANSFERRING) return  // 传输已结束
       ⑤ expectedIdx = receiveBuffer.getExpectedChunkIndex()
       ⑥ if (!receiveBuffer.isReceived(expectedIdx)) {
            // 缺口仍在 → 判丢包
            sendNack(chunk=expectedIdx)    // 发 NACK
            scheduleNack(ctx)              // 续期，再来 100ms
          } else {
            // 缺口补上了 → 什么都不做，定时器链自然终止
          }
     }, 100ms, TimeUnit.MILLISECONDS)
```
### 5.4 取消的 5 条路径
```
方式1 — 缺口补齐 (handleExpectedChunk)
  hasGap()=false → cancelNack()

方式2 — 传输完成 (onTransferComplete)
  所有块落盘 → cancelNack()
方式3 — 连接断开 (channelInactive)
  TCP 连接断开 → cancelNack() + 持久化进度

方式4 — 异常发生 (exceptionCaught)
  任何未捕获异常 → cancelNack() + 持久化进度 + 关闭连接

方式5 — 自然终止 (scheduleNack 回调内部)
  定时器触发时发现缺口已补齐 → 不续期 → 链自然断掉
```

### 5.5 为什么只盯队头
NACK 定时器只关注 expectedChunkIndex（队头缺失块），不为所有缺失块各创建定时器：
```
假设块5和块6同时丢失，块7~10 在缓冲区：
只盯块5:
  块5到达 → 写盘 → advanceAndFlushContiguous()
  → expected 前进到 6 → 发现块6也缺失
  → cancelNack() + scheduleNack()  ← 自动切换盯块6
不需要同时盯多个块：缓冲区里的块（7~10）即使重传了也无法交付，
队头不补齐，后面的块毫无意义
```
### 5.6 为什么要续期
NACK 包本身也可能丢失。如果只发一次 NACK：
```
T=0      数据块5丢失
T=100ms  sendNack(chunk 5) → 这个 NACK 在网络中也丢了！
         → 如果不续期：服务端永远在等，客户端永远不知道
         → 传输永久挂起
```
续期 = NACK 的重传机制：每 100ms 检查一次，只要块没到就再发 NACK，直到到达为止。
---
## 六、溢出写盘策略
### 6.1 问题
缓冲区满时（10 个槽全占），新到达的块如何处理？
```
方案A: 丢弃 → 浪费一次成功的网络传输
方案B: 直接写盘 → 变成随机写，但保留了数据
```
### 6.2 选择方案B
三条写入路径：
```
路径          写入方法          currentOffset     BitSet
───────────   ───────────────  ────────────────  ──────
顺序写入      writeChunk()     推进 ✓            set ✓
溢出写入      writeOverflow()  不动 ✗            set ✓  ← BitSet 标记"已在磁盘"
幂等丢弃      (不写)           不动 ✗            已set ✗
```
### 6.3 关键：currentOffset 不虚报
溢出写入时 currentOffset 不推进，因为 currentOffset 代表"连续已收到的字节数"，不是"散落在磁盘上的字节数"。ACK 用 currentOffset 告诉客户端可以安全清除哪些 pending 块，如果虚报会导致客户端误删未确认的块。
### 6.4 溢出块如何被"链接"回来
```
当队头补齐后，advanceAndFlushContiguous() 向前扫描：
  遇到溢出块 → BitSet=1, slot=null → 跳过（已在磁盘）
  → expectedChunkIndex 继续前进
  
然后 syncContiguousOffset():
  currentOffset 跳跃到新的 expectedChunkIndex 位置
  → 这些溢出块正式计入"连续已接收"范围
```
---
## 七、握手与断点续传
### 7.1 handleHandshake() 五步流程
```
① 状态门: state == AWAITING_HANDSHAKE? → 否则拒绝
② 解析信息:
   fileId = packet.getFileId()
   fileName = packet.getFileName()
   totalFileSize = packet.getNextExpectedOffset()  ← 握手包中复用此字段
③ 断点续传检查:
   FileReceiver.getResumeOffset(outputDir, fileName)
     → 查找 {fileName}.progress 文件
     → 存在 → 读取上次传输的字节偏移 → resumeOffset
     → 不存在 → resumeOffset = 0
④ 初始化 + 回复:
   receiveBuffer = new ReceiveBuffer(1MB)
   receiveBuffer.setExpectedChunkIndex(resumeOffset / 1MB)
   → ctx.writeAndFlush(HANDSHAKE_RESPONSE(resumeOffset, totalFileSize))
⑤ 打开文件:
   new FileReceiver(outputDir, fileName, totalFileSize, resumeOffset)
     → RandomAccessFile 打开目标文件
     → file.setLength(totalFileSize)  // 预分配，防止磁盘碎片
     → file.seek(resumeOffset)        // 跳到续传位置
state = TRANSFERRING

```
### 7.2 进度持久化
```
每 10 个连续块落盘后:
  fileReceiver.persistProgress()
    → 写入 {fileName}.progress 文件
    → 内容: "{currentOffset}" (例如 "52428800" = 50MB)

场景: 传输到 50MB 时进程崩溃
  → 重启后，客户端重新握手
  → 服务端读到 .progress → resumeOffset = 50MB
  → 客户端从 50MB 开始重传
  → 无需重传前 50MB

传输成功完成后:
  fileReceiver.close()
    → 删除 .progress 文件 ← 进度文件无用了
    
```
### 7.3 握手时序
```
客户端                                  服务端
  │                                       │
  │── HANDSHAKE ─────────────────────→    │
  │   (fileName="bigfile.iso",            │  检查 .progress 文件
  │    nextExpectedOffset=1GB)            │  上次传到 50MB
  │                                       │
  │                        receiveBuffer 初始化 (expectedChunkIndex=50)
  │                        fileReceiver 初始化 (seek 到 50MB)
  │                                       │
  │←── HANDSHAKE_RESPONSE ────────────    │
  │    (offset=50MB)                      │
  │                                       │
  │  FileSender 从 50MB 开始读取          │  state = TRANSFERRING
  │  state = SENDING                      │
  │                                       │
  │── DATA(chunk_50, offset=50MB) ────→   │  从 50MB 处开始接收
```
---

## 八、客户端发送设计
### 8.1 非阻塞发送
所有发送通过 Netty 事件循环调度，不使用 Thread.sleep()：
```
start() → scheduleNextSend(0) → doSend()

doSend():
  ① 检查暂停条件: paused? pendingFull? sendComplete?
  ② 读取一块 + MD5 → writeAndFlush
  ③ 暂存到 pendingMap（重传用）
  ④ 检查 channel.isWritable() → false → paused=true
  ⑤ scheduleNextSend(0) → 下一块
```
### 8.2 Pending Chunks（重传缓存）
```
LinkedHashMap<Integer, PendingChunk> (最多 20 个)
收到 ACK(offset=X)  → 移除所有 offset < X 的 pending 块
收到 NACK(chunk=N)  → 从 pending 取 chunk N → 标记 Retransmit → 重发
                      如果 chunk N 已被 evict → 从文件重新读取
```

---
## 九、完整状态机
```
服务端:
  ┌─────────────────┐
  │ AWAITING_        │  收到 HANDSHAKE
  │ HANDSHAKE       │──────────────────┐
  └─────────────────┘                  │
         ▲                             ▼
         │  连接断开           ┌─────────────────┐
         │  客户端重连          │  TRANSFERRING   │
         │  (断点续传)          │                 │
         │                     │  循环:           │
         │                     │  handleData()    │
         │                     │    ↓             │
         │                     │  offer → 3种结果  │
         │                     │    ├ EXPECTED    │
         │                     │    │  → 写盘     │
         │                     │    │  → flush    │
         │                     │    │  → ACK?     │
         │                     │    │  → NACK?    │
         │                     │    ├ BUFFERED    │
         │                     │    │  → 入缓冲    │
         │                     │    │  → 开定时器  │
         │                     │    └ BUFFER_FULL │
         │                     │     → 溢出写盘   │
         │                     │     → 紧急NACK   │
         │                     └───────┬──────────┘
         │                             │
         │                     传输完成 │
         │                             ▼
         │                     ┌─────────────────┐
         └─────────────────────│    COMPLETE     │
              (如需要)          │  关闭文件+连接   │
                               └─────────────────┘
```

---
## 十、文件结构
```
netty-file-transmission/
├── pom.xml
└── src/main/java/com/nettyfile/transfer/
    ├── protocol/
    │   ├── PacketType.java          包类型枚举 (6种)
    │   ├── FileTransferPacket.java   协议包模型 + 工厂方法
    │   └── FileTransferCodec.java   二进制编解码器 (Encoder + Decoder)
    ├── server/
    │   ├── ReceiveBuffer.java       排序缓冲区 (核心：BitSet+HashMap+Deque)
    │   ├── FileReceiver.java        磁盘写入 + 断点续传持久化
    │   ├── ServerChannelHandler.java 服务端逻辑编排
    │   └── FileTransferServer.java  服务端启动引导 (TCP+水位+流量整形)
    ├── client/
    │   ├── RateLimiter.java           令牌桶限流器 (10MB/s)(这个你可以不要了,我的建议是不要再客户端做流控)
    │   ├── FileSender.java            文件读取 + 非阻塞发送 + 重传
    │   ├── ClientChannelHandler.java  客户端逻辑编排
    │   └── FileTransferClient.java    客户端启动引导
    └── util/
        └── MD5Util.java             MD5 校验工具
```

---

## 十一、关键参数汇总

| 参数 | 值           | 依据                               |
|------|-------------|----------------------------------|
| Chunk Size | 1 MB        | 对齐内存页，平衡系统调用频率与丢包风险              |
| TCP Recv Buffer | 600 KB      | BDP(512KB) 上取整留余量                |
| Netty High Watermark | 4 M         | 防止一个块过来就把水位线打满了，造成频繁事件的切换        |
| Netty Low Watermark | 2 M         |                                  |
| 流量整形入站限速 | 10 MB/s     | 对应 80 Mbps 带宽预留，服务端控制            |
| 排序缓冲区 | 10 slots    | 10MB 飞行数据                        |
| ACK 间隔 | 每 10 chunks | 每 10MB 持久化一次进度                   |
| NACK 超时 | 100 ms      | 2 × RTT，超时判定丢包                   |
| Pending Chunks | 20 slots    | 客户端暂存，覆盖服务端 10 槽 + 余量(这里你可以自己设计) |


测试类:NackTestClient,TransferTestTool。因为个人资源有限。这种特定环境下不好测试，所以参数可能会有不准确的